#!/usr/bin/env node

var cluster = require('cluster'),
    argv = require('optimist').argv;

cluster('mobilize')
    .set('workers', argv.workers || 4)
    .set('title', 'Mobilize Master')
    .set('worker title', 'Mobilize Worker')
    .listen(argv.port || 80);

